version: 1
template:
  mappings:
    dynamic_templates:
      - nxlog_text:
          path_match:
            - '*.ContextInfo'
            # nxlog.winev.EventData probably available with nxlog-ee
            - '*.EventData'
            # nxlog.winev.UserData probably available with nxlog-ee
            - '*.UserData'
            # nxlog.winev.HumanMessage should have been renamed to message
            - 'nxlog.winev.HumanMessage'
          match_mapping_type: string
          mapping:
            type: match_only_text

      - nxlog_keyword_and_text:
          path_match:
            - '*.Application'
            - '*.KeyFilePath'
            - '*.*Name'
            - '*.param*'
            - '*.Param*'
            - '*.Payload'
            - '*.PrivilegeList'
            - '*.ProcessPath'
            - '*.updateTitle'
          match_mapping_type: string
          mapping:
            type: keyword
            ignore_above: 1024
            fields:
              text:
                type: match_only_text
