##
## Copyright(C) 2024 @ ZENETYS
## License: MIT (http://opensource.org/licenses/MIT)
##

# Reset property containers. This clears all potential properties created by
# the input module (eg: imjournal systemd properties) so we keep them under
# $.inmeta in case they are needed.
unset $.;
if (exists($!)) then
    set $.inmeta = $!;
unset $!;

include(file=`echo $ZLC_CONFIG_DIR_LOCAL/logs-pre.d/*.conf` mode="optional")
include(file=`echo $ZLC_CONFIG_DIR_LOCAL/logs.d/*.conf` mode="optional")
