if ($.parsing_done != 1 and
    $.app-name startswith "iRule_HTTP_traffic") then {

    # parsing is done in ingest
    set $!event.original = $.msg;

    set $!tags = $!tags & ",rp,f5";
    set $!data_stream.dataset = "rp.f5";
    set $.parsing_done = 1;
}
